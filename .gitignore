# Virtual environments
venv/
.venv/

# Frontend build artefacts / deps
frontend/node_modules/
frontend/dist/

# Python bytecode / caches
__pycache__/
*.pyc

# Generated at seed time -- not source. (The canonical demo ids are NOT here:
# they're fixed constants in scripts/seed.py. Only seed_large.py's randomised
# fingerprint/row-count values get written out.)
scripts/seed_ids_large.py

# Real datasets -- multi-MB, not committed; see README for download instructions
data/*.xlsx

# Env / secrets
.env

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
