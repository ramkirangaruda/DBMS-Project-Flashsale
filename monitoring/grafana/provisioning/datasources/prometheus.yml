apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: flashsale-prom          # dashboards reference this uid
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 2s           # matches the scrape interval
